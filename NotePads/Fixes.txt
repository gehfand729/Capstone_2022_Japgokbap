monster.cs
 getDamage(int damage) - parameter 추가
     [SerializeField] protected GameObject hudDamageText;
    [SerializeField] protected Transform hudPos;
awake에
        hudPos = transform.FindChild("HudDamage").gameObject.GetComponent<Transform>();
추가

각각의 monster 스크립트
 getDamage에
        GameObject hudText = Instantiate(hudDamageText);
        hudText.transform.position = hudPos.position;
        hudText.GetComponent<DamageTextTest>().damage = damage; 
추가

각각의 monster의 자식으로 hudPos를 위한 빈 오브젝트 추가
이름: HudDamage

StageManager.cs
 "Objects"에 public GameObject itemClones

ObjectHit.cs(생성)



Scene
itemClones 생성(생성되는 item은 itemClones의 하위 오브젝트로 들어감.)